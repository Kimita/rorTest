# README

* Ruby version
    * 2.4.5

* System dependencies
    * when `bundle install`
        * libsqlite3-dev
    * when `bundel exec bin/rails s`
        * node

* Configuration
    * No info.

* Database creation

```SQL
CREATE DATABASE `bookshelf` /*!40100 DEFAULT CHARACTER SET utf8 COLLATE utf8_unicode_ci */
```

* Database initialization

```bash
$ bundle exec bin/rails db:migrate RAILS_ENV=development
```

* How to run the test suite
    * No info.

* Services (job queues, cache servers, search engines, etc.)
    * No info.

* Run Debug

```bash
$ bundle exec bin/rails s
```

* Deployment instructions
    * No info.

* ...
