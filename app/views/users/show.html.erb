<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

<hr />

<table>
  <thead>
    <tr>
      <th>post ID</th>
      <th>contents</th>
    </tr>
  </thead>

  <tbody>
    <% @microposts.each do |post| %>
      <tr>
        <td><%= post.id %></td>
        <td><%= post.content %></td>
      </tr>
    <% end %>
  </tbody>
</table>

